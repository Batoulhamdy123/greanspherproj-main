import 'package:flutter/material.dart';

class HomePageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      // appBar: AppBar(),
      // body: ElevatedButton(onPressed: () {}, child: Text("hello my dear")),
    );
  }
}
